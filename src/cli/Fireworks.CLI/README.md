# Fireworks.CLI

Command-line interface for the Fireworks SDK generated with AutoSDK.

## Installation

```bash
dotnet tool install --global Fireworks.CLI --prerelease
```

## Usage

```bash
fireworks --help
fireworks api --help
```